# Test Environment
# Used for CI/CD and local testing

# Database (Local test uses same PostgreSQL instance, different database)
DATABASE_URL_TEST=postgres://bentillett:@localhost:5432/nextjs_dashboard_test

# NextAuth (dummy values for testing)
AUTH_SECRET=test-secret-key-for-ci-testing-only
AUTH_URL=http://localhost:3000

# Set environment
NODE_ENV=test