# Test Environment
# Used for CI/CD and local testing

# Database (Local test uses same Docker container, different database)
DATABASE_URL_TEST=postgres://dev_user:dev_password@localhost:5433/nextjs_dashboard_test

# NextAuth (dummy values for testing)
AUTH_SECRET=test-secret-key-for-ci-testing-only
AUTH_URL=http://localhost:3000

# Set environment
NODE_ENV=test